spring:
  application:
    name: post-backend

  datasource:
    url: jdbc:mysql://localhost:3306/board_db
    username: root
    password: dudwls0918
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        highlight_sql: true

server:
  port: 8080